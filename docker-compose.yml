services:
  server:
    build: ./server
    ports:
      - "0.0.0.0:8080:8080"
    environment:
      SERVER_PORT: "8080"
      SERVER_PIN: "1234"
      SERVER_HOST: ${SERVER_HOST:-}
    volumes:
      - ./data:/app/data
