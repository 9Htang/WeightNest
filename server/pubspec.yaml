name: weightnest_server
description: WeightNest 中央服务器 — Dart shelf + PostgreSQL
version: 1.0.0
environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_cors_headers: ^0.1.5
  postgres: ^3.4.0
  uuid: ^4.5.3
  crypto: ^3.0.0
