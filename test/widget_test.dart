import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds', (tester) async {
    // Integration tests in integration_test.dart cover the full app
    expect(1 + 1, 2);
  });
}
